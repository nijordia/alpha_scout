on:
  schedule:
    # Debug schedule - run every 15 minutes for testing
    - cron: '*/15 * * * *'
    
    # Regular schedules (commented out during testing)
    # - cron: '30 20 * * 1-5'  # During daylight saving
    # - cron: '30 21 * * 1-5'  # During standard time
  
  # Allow manual triggering for testing
  workflow_dispatch: